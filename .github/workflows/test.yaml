---
name: Build Docker Image

on:
 pull_request: 
  branches: master

jobs:
  deploy:
    name: Build Application
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
  test:  go test

